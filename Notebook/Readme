This is the IPNYB file containing the code. I have tried to make the code function even when the IPNYB and the source data files are in separate folders. But if there are errors, load the repository in your machine and it should work just fine
